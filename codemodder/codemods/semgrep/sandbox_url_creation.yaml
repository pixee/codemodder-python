rules:
  - id: sandbox-url-creation
    # TODO: need to fix this rule
    patterns:
      - pattern: requests.get(...)
      - pattern-inside: |
          import requests
          ...
    message: Unbounded URL creation
    languages:
      - python
    severity: WARNING
