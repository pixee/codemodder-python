rules:
  - id: sandbox-url-creation
    pattern-either:
      - patterns:
        - pattern: requests.get(...)
        - pattern-inside: |
            import requests
            ...
    message: Unbounded URL creation
    languages:
      - python
    severity: WARNING
