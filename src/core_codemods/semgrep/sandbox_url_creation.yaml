rules:
  - id: sandbox-url-creation
    message: Unbounded URL creation
    severity: WARNING
    languages:
      - python
    pattern-either:
      - patterns:
        - pattern: requests.get(...)
        - pattern-inside: |
            import requests
            ...
